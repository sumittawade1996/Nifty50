# Railway.app deployment config (free hosting)
# Deploy your Nifty SIP Bot for free on Railway

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python nifty_bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
